services:
  - type: web
    name: mzansigo-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
      - key: ADMIN_PROMOTE_SECRET
        sync: false
