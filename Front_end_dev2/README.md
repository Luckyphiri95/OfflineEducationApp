# Offline Education App

A clean React Native / Expo scaffold for the Front-end Developer 2 deliverables.

## Setup

1. Install Node.js: https://nodejs.org/
2. Open terminal in this folder:
   ```bash
   cd "c:\Users\Siyanda Mthethwa\OneDrive\Desktop\Trial"
   npm install
   npm start
   ```

## GitHub commit and push

```bash
cd "c:\Users\Siyanda Mthethwa\OneDrive\Desktop\Trial"
git init
git add .
git commit -m "Add front-end dev 2 deliverables and Expo scaffold"
git branch -M main
git remote add origin https://github.com/Luckyphiri95/OfflineEducationApp.git
git push -u origin main
```

If the repository already exists locally, skip `git init` and just use `git add .`, `git commit`, and `git push`.
